repo: "https://raw.githubusercontent.com/Tc554/loader-releases/main"
github_token: "your_github_token_here"
general_delay: 1000  # Delay in milliseconds between plugin loads
loadables:
  example:
    jarName: "example"
    priority: 1
    enabled: true